object question1{
    def main (args:Array[String]): Unit={
        var k,i,j = 2
        var m,n = 5
        var f = 12.0f
        var g = 4.0f
        var c = 'X'

        val A = k + 12 * m
        val B = m/j
        val C = n%j
        val D = m/j * j
        val E = f + 10 * 5 + g
        i+=1
        val F = i * n

        println(s"a)$A")
        println(s"b)$B")
        println(s"c)$C")
        println(s"d)$D")
        println(s"e)$E")
        println(s"f)$F")
    }
    
}

// 1.  Scala is a mixture of both object oriented and functional programming.
//     Java is a general purpose object oriented language.

// 2.  Scala is less readable due to nested code.	
//     Java is more readable.

// 3.  The process of compiling source code into byte code is slow.	
//     The process of compiling source code into byte code is fast.

// 4.  Scala support operator overloading.
//     Java does not support operator overloading.

// 5.  Scala variables are by default immutable type.
//     Java variables are by default mutable type.